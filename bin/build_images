#!/bin/bash

set -e -x

image=${1:-all}

if [[ ${image} == "all" ]]; then
    images=(cpu)
else
    images=(${image})
fi

for img in "${images[@]}"
do
    echo ">>>> building ${img} image"
    pushd containers/${img}_image
    docker build \
      -t ludwig_${img} .
    popd
done

